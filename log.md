# Change Log

Append-only. Newest at the bottom. Each entry: `## [YYYY-MM-DD] <op> | <id> — <note>`
where `<op>` ∈ {add, update, deprecate, lint}. `grep "^## \[" log.md | tail` shows recent activity.

## [2026-06-12] add | wiki — Initialized standards wiki: schema (CLAUDE.md), index, log, template.
## [2026-06-12] add | nextjs — Seed page (skeleton, status draft).
## [2026-06-12] add | nextjs-with-payload — Seed page (skeleton, status draft).
## [2026-06-12] add | add-mui-to-nextjs — Seed page (skeleton, status draft).
## [2026-06-12] add | add-shadcn-to-nextjs — Seed page (skeleton, status draft).
## [2026-06-12] add | add-antd-to-nextjs — Seed page (skeleton, status draft).
## [2026-06-12] add | listing — Seed page (skeleton, status draft).
## [2026-06-12] add | naming — Seed page (skeleton, status draft).
## [2026-06-12] update | nextjs — Authored full standard from ai-driven-experience (structure) + example-payload (data layer). Decided via grill session: flat src/, (public)/(protected) route groups, thin page.tsx → feature pages/, full feature subfolder vocab (on-demand), default-export components + barrel rules, RQ+axios data layer (service→hook→transform), vitest .spec.ts + __mocks__, tooling baked in. CSS concerns deferred. status → stable.
## [2026-06-12] update | naming — Converted to thin pointer to [[nextjs]]. status → stable.
## [2026-06-12] update | nextjs — Removed feature-root barrel rule. Features have NO root index.ts; import directly from the subfolder barrel (@/features/x/components, /hooks, /interfaces) — reference the folder type, not the feature.
## [2026-06-12] update | add-shadcn-to-nextjs — Authored full standard from ai-driven-experience. Owns the entire shadcn UI + token layer (the CSS-framework boundary nextjs defers): Tailwind v4 CSS-first (no tailwind.config), components.json (new-york, baseColor stone, utils → @/libs/utils.lib), cn() in libs/utils.lib.ts, postcss, hand-written 3-tier design tokens in src/theme (colors.css primitives + tokens.css semantic/component via @theme), globals.css shadcn var mapping via @theme inline, ui/ flat-files + export * + cva (.variants.ts for JS-side hex maps), shared-components rule, design-system showcase routes, framer-motion, Tailwind IntelliSense. Token rules: never primitives/hex in components, Tier3 only when Figma defines it. status → stable.
## [2026-06-12] update | wiki — Decision (grill): design tokens are handled PER CSS FRAMEWORK; NO shared/agnostic design-tokens standard. Each add-{shadcn,mui,antd}-to-nextjs owns its token mechanism (shadcn = CSS vars + Tailwind @theme; MUI/antd to define their own). Earlier-explored options (neutral DTCG source + Terrazzo generation) were rejected in favor of matching each framework's native approach.
## [2026-06-16] lint | wiki — Health-check. All [[id]] links resolve; requires/conflicts_with point at real ids; id==filename for all 7; index.md matches files + statuses. Fixed one contradiction: nextjs base labelled components/ui/ as "primitive components" (shadcn-centric) but MUI/antd put COMPOSITE components there (primitives come from the lib directly) — reworded the base's tree comment + CSS-boundary bullet to be framework-neutral (bumped nextjs updated). No orphans, no broken links, no draft-in-use needing promotion (listing + nextjs-with-payload are still genuine skeletons w/ TODOs). Flagged for human (not auto-fixed): (1) design-system showcase-routes convention is duplicated verbatim across all 3 UI standards — candidate for its own pattern page; (2) ThemeTokens interface shape is duplicated in add-mui + add-antd theme.d.ts — candidate for a shared token-name catalog reference (mechanism stays per-framework per existing decision).
## [2026-06-16] update | add-antd-to-nextjs — Authored full standard (antd v5 + antd-style; no company reference repo — grounded in antd/antd-style community best practice via context7). Decided via grill session: flat src/ + same token practice as [[add-mui-to-nextjs]]/[[add-shadcn-to-nextjs]]; TWO layers — antd native Seed→Map→Alias (theme.token + algorithm, fed from colors.ts) = the on-brand sync for DEFAULT antd components, and antd-style customToken = our Figma-named Tier2 semantic/Tier3 component layer (== MUI theme.tokens.*) for authored styling; HYBRID palette derivation (seed + keep algorithm, pin Map/Alias only where Figma explicit — exact fidelity comes from customToken, not from fighting the algorithm); colors.ts = only place hex lives; ThemeTokens interface + `declare module 'antd-style' { interface CustomToken extends ThemeTokens }` so useTheme()/createStyles token are typed; per-component theme.components split into *.theme.ts (button.theme.ts) mapping tokens→antd slots; app styling via createStyles in .style.ts consuming token.<figma.path> (antd alias tokens allowed for structural/layout only); SSR via antd-style StyleRegistry (extractStaticStyle + useServerInsertedHTML) — NOT @ant-design/nextjs-registry (shared cssinjs cache, avoid double); AppThemeProvider = antd-style ThemeProvider(theme=themeConfig, customToken=tokens, themeMode light); React19 patch gotcha; design-system showcase routes. status → stable.
## [2026-06-16] update | add-mui-to-nextjs — Authored full standard from example-payload (MUI + Emotion). Decided via grill session: flat src/ (borrow content, not the app's src/shared/ layout); 3-tier tokens mirroring [[add-shadcn-to-nextjs]] but as a typed theme.tokens.* object (Tier1 primitives in colors.ts → palette synced for MUI defaults + tokens.theme.ts for Tier2 semantic/Tier3 component, both built from colors.ts); ThemeTokens declared GLOBALLY + merged onto Theme/ThemeOptions so useTheme().tokens/sx are typed; HARD rule: overrides (*.theme.ts callback form) and *.style.ts consume theme.tokens.* only — never palette scales/slots, never raw hex (reference's palette.error[700] etc. flagged as the anti-pattern to fix); MUI primitives used directly from @mui/material (no components/ui wrapper), composite UI in components/ui/<Name>/ + .style.ts; AppRouterCacheProvider (server layout) → client ThemeProvider; custom breakpoints (mobile/tablet/compact/desktop/wide, defaults off) + variants (Button/Chip/Typography); design-system showcase routes. theme.d.ts → src/types/. status → stable.
## [2026-06-16] update | nextjs-with-payload — Authored full standard from example-payload. Decided via grill session: overrides [[nextjs]] flat src with 3 domain roots (frontend/payload/shared; frontend+shared scoped-reference, not re-documented); requires [[nextjs]]. Mandated core stack (Postgres+uuid, S3, pino logger, migrations, zod env), localization/seo/cloud/OTel/plugins optional. Full data-layer recipe: adminAxiosInstance built-in (adminPayloadGet*) vs custom endpoints (defineEndpoint+pipeHandlers+withAuth/Logger/ClientInfo → server/handlers Local API, useAdminGet*); external-core/BFF proxy documented as optional variant. Full migrations/seeds/scripts (dual-export seed + bin + pnpm + onInit), collections conventions, env config, (payload) mount + generate:types/importmap, custom field plugins. Neutral example domain. Admin theme deferred to [[add-payload-theme]]. status stays draft.
## [2026-06-16] add | form — Authored form pattern (category pattern; requires [[nextjs]]) from clo-portal forms.md + example-payload. Decided via grill session: react-hook-form ^7 + zod ^3.25 (v3 message API) + @hookform/resolvers ^3 + next-intl ^4. Interface-first (interface {Entity}FormFields = source of truth, schema annotated ZodType<Fields>); schema built INSIDE a use{Form}FormSchema hook for i18n messages (so no z.infer — documented as the reason). One hook per form, single schema. Controller/useController for every field (never mix with register); defaultValues always; value ?? '' guard. FormProvider+useFormContext only when large/reused. Submit via RQ mutation→service with onSubmit(fields,setError) mapping API field errors, generic→snackbar. Shared error.form.* keys + per-form namespace. MUI-illustrated (antd/shadcn swap inputs). Colocated schema-hook .spec.ts. i18n setup deferred (TODO: future i18n standard). status draft.
## [2026-06-16] update | nextjs — Added [[form]] to Related standards (back-link).
## [2026-06-18] add | code-quality-tooling — Authored convention (requires [[nextjs]], applies_to [nextjs]) from example-payload. Decided via grill: ONE standard covering eslint + prettier + husky + lint-staged + .vscode (not split, not an integration); nextjs's Tooling & config section shrunk to a pointer (scripts dev/build/start/type-check stay in nextjs; lint/lint:fix/format/format:check/prepare move here). ESLint = full hand-rolled flat-config v9 documented VERBATIM (6 plugins, recommended spreads + custom rules incl. max-depth:2 + no-nested-ternary as deliberate company choices, consistent-type-imports/exports, import/order, no-unused-vars ^_, the disabled rules, and the exclude-then-relax scripts/** override). Prettier verbatim (singleQuote, no semi, trailingComma all, printWidth 100). Kept cross-env NODE_OPTIONS script wrappers; DROPPED pre-commit index.lock cleanup, cSpell dictionary, vitest.explorer + launch.json (this standard owns extensions.json = eslint+prettier only, settings.json = formatOnSave+eslint fixAll). Hooks: pre-commit=lint-staged, commit-msg=PascalCase <Type>: msg, pre-push=type-check + lowercase branch-name regex. Git rules (commit types Fix/Feat/Docs/Style/Refactor/Perf/Test/Chore +Merge; branch prefixes feature/bugfix/hotfix/release/dev/setup/base/refactor/chore/story) documented verbatim here FOR NOW with a gotcha noting the casing mismatch + future migration to a git-workflow convention. Versions = major-only pins. Payload-specific eslint ignores + @payload-config alias + next-intl import/ignore + prettier paths live here under a "When using Payload" callout. status draft.
## [2026-06-18] update | nextjs — Tooling & config section converted to a pointer to [[code-quality-tooling]] (kept the dev/build/start/type-check script ownership note).
